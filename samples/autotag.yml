policies:
  - name: azure-auto-tag-creator
    resource: azure.resourcegroup
    description: |
      Tag all existing resource groups with the 'CreatorEmail' tag
    actions:
     - type: auto-tag-user
       tag: CREATED-BY
       update: true
       days: 90
     - type: untag
       tags: ['Creator', 'CREATED-BY2']