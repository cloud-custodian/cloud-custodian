---
name: ec2-userdata
resource: ec2
filters:
- or:
  - type: user-data
    op: regex
    value: (?smi).*(ch)?passw(or)?d(?! --) # basic, but hey.
  - type: user-data
    op: regex
    value: (?smi).*A[KS]IA