language: python
env:
  - AWS_DEFAULT_REGION="us-east-1"
  # Bypass credential lookup
  - AWS_ACCESS_KEY_ID="some"
  - AWS_SECRET_ACCESS_KEY = "thing"
  
python:
  - "2.7"
install:
  - pip install -r requirements-dev.txt
  - python setup.py develop
script: nosetests -s -v --with-timer tests



